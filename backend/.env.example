# ===========================================
# ITC Shield / TaxPay Guard - Environment Variables
# ===========================================

# DATABASE
# Uncomment and set to use PostgreSQL (Supabase/Render/Railway)
# If not set, SQLite is used (local development)
# DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT].supabase.co:5432/postgres

# SECURITY
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
# JWT_SECRET_KEY=your-secret-key-here

# GSP API (Sandbox.co.in / Zoop)
# GSP_MODE=mock (default) OR sandbox
GSP_MODE=mock
SANDBOX_CLIENT_ID=your_key_live_...
SANDBOX_SECRET=your_secret_...
